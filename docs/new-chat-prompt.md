# Prompt For A Fresh Codex Chat

Copy this into a new chat when the current one becomes too heavy.

```text
Привет. Продолжаем проект L2Realm.

Проект лежит локально тут:
D:\Проекты\L2Realm

Сайт:
https://l2realm.ru

Стек:
Next.js 16 frontend, NestJS backend, Prisma, PostgreSQL, Docker Compose, nginx, VK ID auth, YooKassa.

Сначала обязательно прочитай:
1. D:\Проекты\L2Realm\AGENTS.md
2. D:\Проекты\L2Realm\docs\codex-quick-context.md

Не читай всю историю и весь репозиторий без необходимости. Сначала работай точечно через rg и открывай только файлы задачи.

Главные правила:
- Никогда не деплой без моего прямого подтверждения.
- Если есть Prisma migration, перед деплоем нужен бэкап базы.
- Не ломай Markdown/переносы/пробелы в статьях и описаниях серверов.
- Не меняй публичные URL без отдельного решения и редиректов.
- Не трогай .env и секреты без причины.
- Не возвращай публично VK full name, только nickname/avatar.
- SEO, sitemap.xml, robots.txt и metadata не ломать.
- После frontend/UI правок проверяй `frontend npm.cmd run build` и `git diff --check`.

Последний известный продакшен:
commit f30274f Harden legal pages and refresh blog

Что важно по сайту:
- Каталог сортируется VIP -> Сервер недели -> BOOST -> остальные по голосам.
- Вкладка Скоро открытие: VIP-открытия должны быть выше обычных, внутри сортировка по дате открытия.
- Голосование ограничено IP и аккаунтом на 24 часа.
- Vote Manager API для админов серверов:
  https://l2realm.ru/api/vote/check?server_id=ID&nickname=НИК
- На странице проекта есть вкладки Обзор / Информация / Сервера / Отзывы.
- Статьи могут привязываться к проектам через Article.serverIds.
- Скачивание клиентов/патчей/апдейтеров намеренно убрано из публички и админки из-за юридических рисков.
- Яндекс.Метрика и VK Pixel отключены, чтобы privacy/legal не противоречили HTML.

Текущая ближайшая задача:
Доработать /blog:
- убрать жёлтую плашку с середины страницы;
- сетку статей сделать 3 колонки вместо 4;
- убрать верхнюю главную/featured статью;
- порядок блоков: сначала Обзоры, потом Новости, потом остальные разделы;
- Новости должны выглядеть как обычные карточки;
- правый блок категорий/скоро открытие/свежие статьи должен двигаться вместе со страницей, не быть sticky;
- убрать плашку "Не пропусти старт! Следи за будущими открытиями...".

Вероятные файлы:
- frontend/src/app/blog/page.tsx
- frontend/src/app/blog/page.module.css

Перед финалом:
- проверь git status;
- сделай frontend build;
- сделай git diff --check;
- не деплой, пока я не скажу.
```
